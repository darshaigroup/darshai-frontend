# TODO: Resolve GeoWellnessCenter “merge conflict”/schema issue

- [x] Inspect `src/pages/OurProgram/GeoWellnessCenter.jsx` for conflicting data schema vs modal UI
- [x] Unify centre data model used by modal (`title`, `subtitle`, `factor`, `intervention`, `image`, `description`)
- [x] Add click handling: selecting a centre now opens the modal
- [x] Render Forest/Coastal/Mountain centre grids using a reusable `CentreCard`
- [ ] Run build/lint to verify (note: current terminal command quoting on Windows CI may need adjustment)

